apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: egress-tcp-test-02
  namespace: istio-egress
spec:
  selector:
    istio: egressgateway
  servers:
  - port:
      number: 443
      name: tls-port
      protocol: TLS
    hosts:
    - 'tcp-test-02.omegaworld.net'
    tls:
      mode: ISTIO_MUTUAL
